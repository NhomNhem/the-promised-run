# Dev Log - 26/04/2026

## Hôm nay mình đã làm được gì?

### 1. Fix cấu trúc Player
- Mình tách Player ra làm 3 phần:
  - **Player** (root): chứa PlayerController, Rigidbody, CapsuleCollider
  - **Visual**: chứa Animator + Knight model
  - **Detector**: dùng để detect mặt đất (đổi từ trigger sang RaycastPro)

- Fix cái vụ Rigidbody lăn tròn =)) 
- Fix bằng cách set FreezeRotationX + FreezeRotationZ trong Rigidbody constraints

### 2. Setup Animation Controller
- Tạo Player.controller mới với:
  - 5 parameters: VelocityX, VelocityZ, IsGrounded, JumpTrigger, VerticalVelocity
  - Locomotion state dùng 2D BlendTree (Idle → Walk → Run)
  - Jump state gán Jump_Start.anim
  - Overload state (tạm để trống, cần gán Hit_A)

- Fix type của parameters trong Animator:
  - IsGrounded: Float → Bool
  - JumpTrigger: Float → Trigger

- Thêm transition Locomotion → Jump (không có exit time, có condition JumpTrigger)

### 3. Code PlayerController
- Thêm LINQ (sẽ chuyển sang ZLinq khi package resolve) để tìm Visual và Detector tự động:
  ```csharp
  // ZLinq (khi resolve):
  // detector = transform.Children().FirstOrDefault(c => c.name == "Detector");
  
  // LINQ hiện tại:
  detector = Enumerable.Range(0, transform.childCount)
      .Select(i => transform.GetChild(i))
      .FirstOrDefault(c => c.name == "Detector");
  ```

- Thêm RaycastPro BasicRay để detect đất:
  - Nếu có BasicRay thì dùng `groundRay.Performed`
  - Nếu không thì fallback về trigger cũ

### 4. Packages
- Thêm **ZLinq** vào manifest.json (đang chờ resolve)
- Dự định dùng ZLinq thay LINQ cho performance tốt hơn với Unity

---

## Còn cần làm gì?

1. Xóa transition trùng trong Animator (cái có ExitTime)
2. Gán animation vào Overload state
3. Set BasicRay Detect Layer = Ground
4. Test lại trong Play mode

---

## Các lỗi đã fix
- YAML parser lỗi → tạo lại controller bằng MCP
- Duplicate Animator → restructure lại hierarchy
- Rigidbody lăn → set constraints
- Detector lệch vị trí → set (0, -1, 0)

---

## Ghi chú
- RaycastPro bị lỗi init nhưng không ảnh hưởng, chỉ là warning
- Animator mới tạo có vấn đề parsing, phải tạo lại nhiều lần

---

Mong mai test được hết các chức năng cơ bản: di chuyển, nhảy, camera follow.